---

# **Calculadora de Modelos Matemáticos y Temperatura de Equilibrio**

Este repositorio contiene el programa **ProyectoFinalFisica**, una herramienta desarrollada en lenguaje C para calcular:

* Modelos matemáticos relacionados con el tema de capacidades térmicas específicas
* Temperatura de equilibrio entre dos sustancias líquidas

El objetivo del programa es automaitzar y optimizar cálculos.

---

## **Ejecutable del programa**

El archivo:

```
ProyectoFinalFisica.exe
```

es la versión **completamente funcional** del programa.

Para usarlo:

1. Se necesita descargar el archivo `.exe`
2. Abrirlo directamente en Windows: *(no requiere instalación adicional)*

El programa correrá de inmediato y mostrará su menú de opciones.

---

## **Código fuente**

En este repositorio también se incluye:

```
ProyectoFinalFisica.c
```

Aquí se encuentran **todas las líneas de código** que codificamos para desarrollar el programa, totalmente comentadas y legibles.

Si se desea compilarlo, con **GCC** es necesario utilizar los siguientes comandos:

### **Compilación**

```bash
gcc ProyectoFinalFisica.c -o ProyectoFinalFisica
```

### **Ejecución**

```bash
.\ProyectoFinalFisica
```

---

## 📁 **Contenido del repositorio**

```
📦 Proyecto_Fisica
 ├── ProyectoFinalFisica.c
 ├── ProyectoFinalFisica.exe
 └── README.md
```

---

## **Notas**

* El archivo `.exe` funciona únicamente en **Windows**.
* Para compilar manualmente el código fuente se requiere:

  * GCC (MinGW, TDM-GCC o similar)
  * Terminal con soporte UTF-8 para los caracteres especiales del programa

---

